{
  "name": "menu-restaurante-react-native",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo simples para app Expo em ./app",
  "scripts": {
    "install:app": "npm install --prefix app",
    "start": "npm run start --prefix app",
    "start:tunnel": "npm run start:tunnel --prefix app",
    "start:lan": "npm run start:lan --prefix app",
    "start:localhost": "npm run start:localhost --prefix app",
    "web": "npm run web --prefix app",
    "android": "npm run android --prefix app",
    "ios": "npm run ios --prefix app",
    "tsc": "npm run tsc --prefix app",
    "pdf": "md-to-pdf docs/relatorio-projeto.md --document-title 'Relatório do Projeto' --stylesheet docs/pdf.css --config-file docs/md-to-pdf.config.json"
  },
  "devDependencies": {
    "md-to-pdf": "^5.2.4"
  }
}
